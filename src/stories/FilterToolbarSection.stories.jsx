import React from 'react';
import { FilterToolbarSection } from './../components/controls/FilterToolbarSection';

export default {
  title: 'Marketplace/FilterToolbarSection',
  component: FilterToolbarSection,
};

const Template = (args) => <FilterToolbarSection {...args} />;

export const Default = Template.bind({});

Default.args = {};
